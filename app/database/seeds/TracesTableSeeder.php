<?php

class TracesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('traces')->truncate();

		$traces = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('traces')->insert($traces);
	}

}
