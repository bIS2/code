<?php

class RevisedsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('reviseds')->truncate();

		$reviseds = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('reviseds')->insert($reviseds);
	}

}
